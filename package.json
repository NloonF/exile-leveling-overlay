{
  "name": "exile-leveling",
  "engines": {
    "node": ">=22.12.0"
  },
  "workspaces": [
    "common",
    "seeding",
    "web"
  ],
  "scripts": {
    "fake-helper": "node tests/fake-log-api.mjs",
    "test": "node --import tsx --test tests/*.test.mts",
    "tauri": "tauri",
    "build:windows": "tauri build --bundles nsis"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "prettier": "^3.9.5",
    "tsx": "^4.23.1",
    "ws": "^8.18.3"
  }
}
